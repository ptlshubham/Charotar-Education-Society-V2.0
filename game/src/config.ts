// The only link back to the parent website. Change this when deploying elsewhere.
export const gameConfig = {
  schoolUrl: 'https://cesociety.in/',
  storageKey: 'ces-learning-quest-v1',
  questionsPerRound: 5,
  pointsPerAnswer: 20,
} as const;
